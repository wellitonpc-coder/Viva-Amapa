import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vivaamapa.turismo',
  appName: 'Viva Amapá',
  webDir: 'dist',
  server: {
    cleartext: true
  }
};

export default config;